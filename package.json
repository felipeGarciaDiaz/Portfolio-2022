{
  "name": "Poler",
  "version": "1.0.0",
  "scripts": {
    "start": "npm run build-client && node server",
    "start-local": "npm run build-client && PORT=5051 node server",
    "build-client": "cd client && npm run build && cd .."
  },
  "dependencies": {
    "express": "^4.18.1",
    "mongoose": "^6.3.0",
    "nodemailer": "^6.7.7",
    "socket.io": "^4.5.1"
  }
}
